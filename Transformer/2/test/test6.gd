@tool
extends EditorScript


# Called when the script is executed (using File -> Run in Script Editor).
func _run():
	var itteration:int = 10
	var numbers:PackedFloat64Array
	numbers.resize(itteration)
	var sum:float
#	for i in range(1000):
#		sum += randfn(1.5, 0.5)
#	print(sum/1000.0)
#	for i in range(100):
#		numbers[i] = randfn(1.5, 0.5)
#	for i in range(itteration):
#		print(randfn(1.0, 0.1) + randi_range(0, 1) * -1)
#	for i in range(itteration):
#		print(randfn(1.0, 0.3))
	
