#include <iostream>
#include <string>
#pragma once


class CompilerEngine {



};

void split_string(std::string s, char delimiter, uint64_t max){
    int count = s.length();
    for (size_t i = 0; i < count; i++)
    {
        
    }
    
}

class _syntax {
    int type;
};

enum SYNTAX_TYPE {
    KEYWORD,
    OPEN,
    CLOSE,
    OPENCLOSE
}